// use instead of filter to find only the first match 
const nums = [1,2,3,4,5];
const result = nums.find(num => num > 3);
console.log(result);
//---------------------------

const numss= [2,4,6,8,10];
const final=numss.find(num=>num>5);
console.log(final);