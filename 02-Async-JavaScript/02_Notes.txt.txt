JS Object
   ↓ stringify
JSON String
   ↓ Internet/API
JSON String
   ↓ parse
JS Object
This cycle happens thousands of times in backend applications.
------------------------------------------------------------------
js main hota kya hai na ki time out main pehle baki ke tasks hote hain then time out hokar hi executr hota hai timeout function but in cpp pehle vo execute hota hai example 3 sec ke baad then vo baad main propgram execute hoga line by line
To JS us callback ko schedule kar deta hai.
set time out ka matlab At least 3 seconds baad callback ko run karna na ki 3 swconds baad kar hi dena hai esa 
and execute bhi 3 seconds minimum ke baad hi hota hai callback
--------------
Callback = Function passed as an argument to another function, which is executed later.
---------------
Promise ka kaam: "Main future me result dunga."
--------------------
resolve() → then()
reject()  → catch()
--------------------------
Dono synchronous code ke baad hi chalte hain.
Bas Promise callbacks ko JavaScript zyada priority deta hai setTimeout ke compare kare agar toh 
---------------------------
promise main jo return karoge, wahi next .then() ko milega
------------------
Lekin bade projects me 10-15 .then() aa jaye to readability kam ho jati hai.
Isliye modern JS me:
async function getData() {
    const user = await loginUser();
    const profile = await getProfile(user);
    const posts = await getPosts(profile);

    console.log(posts);
}
------------------------------
async function value deta hai -----  promise(yahan jo bhi return kiya hai vo)
Every async function ALWAYS returns a Promise.
-----------------------------
async function test() {
    return 100;
}
Internally JS isko lagbhag aise treat karta hai:
function test() {
    return Promise.resolve(100);
}
----------------------------------
Meaning of await "Promise complete hone ka wait karo, phir uske andar wali value de do."
------------------
Current async function pause
↓
Main program continues
↓
Promise complete
↓
Function resumes
Means ki like agar unctuion main awairt hai togh aapko us function se bahaer nukalna padega then main fuyn ction main jana padega thne vo dobara se function ke andar aayega
-------------------------------------
const add = (a,b) => a+b;
const sub = (a,b) => a-b;
console.log(add(10,20));
esa bhi hota hai yaad rakhna vasie hi 
---------------------------------
export matlba public bana dena
---------------------
Ek file me Multiple Default Exports nahi ho sakte