import { add } from "./math.js"; // Syntax !!!
console.log(add(10,20));
import { sub } from "./math.js"; // Syntax !!!
console.log(sub(10,20));
import { mul } from "./math.js"; // Syntax !!!
console.log(mul(10,20));


import panda from "./math.js"; // yajan kuch bhi naam use karlo default ke liye kyunioi defalut toh ek hi rahega hamesha hi
console.log(panda(10,20));
